FROM mcr.microsoft.com/dotnet/aspnet:8.0 AS runtime
WORKDIR /app

# Copy the published application files
COPY bin/Release/net8.0/ ./

# Expose gRPC port
EXPOSE 5249

# Set the entry point
ENTRYPOINT ["dotnet", "RPCService.dll"]